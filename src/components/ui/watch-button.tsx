import React from "react";
import { Button } from "react-bootstrap";

const WatchButton = () => {
  return (
    <Button
      style={{
        backgroundColor: "#e71d61",
        border: "none",
        color: "white",
        padding: "0.5rem 2rem",
       
      }}
    >
      Watch Now
    </Button>
  );
};

export default WatchButton;
